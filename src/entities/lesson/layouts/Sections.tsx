import SectionsList from '../components/SectionsList'

const Sections = () => {
  return (
    <div
      className="info-65 border-radius p-20"
      style={{
        backgroundColor: 'var(--medium-blue)',
      }}
    >
      <SectionsList />
    </div>
  )
}

export default Sections
